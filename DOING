+ Write a professional CMakeLists.txt
- Add SSL/TSL Layer between TCP and IP(in libevent).
- Change the Database into Redis.
- Use the Log of Linux.
- Change into a daemon.
- Add the "chroot" command.
